# Minus Skill 项目

## 开发规范
- Skill 结构设计采用三步法（输入→步骤→输出），由 Plugin 内置引导完成
- 每个节点开发按四个维度（数据需求→处理逻辑→输出→确认）
- 数据获取使用直接 API 调用（HTTP fetch），不依赖 MCP
- 输出使用 minus.output.* 工具渲染

## 代码三层模型
每个步骤的代码包含三层：
1. 数据获取层（确定性，HTTP API 直调）
2. 数据处理层（确定性代码 或 LLM 调用）
3. 输出渲染层（确定性，minus.output.* 工具）

## 测试
- 本地预览地址：http://localhost:${PREVIEW_PORT}/preview
- 修改代码后预览会自动更新
- 发布前需完整运行一遍 pipeline 验证

## 当前项目状态
!`cat .minus.json 2>/dev/null || echo "项目未初始化"`
