# Minus Creator Plugin

本项目的设计原则、开发规范、测试要求与项目结构**以 `CLAUDE.md` 为唯一权威定义**（指令单源化原则），本文件不复制其内容——请直接阅读 [CLAUDE.md](CLAUDE.md)。

快速入口：

```bash
bash tests/run-all.sh   # 全部测试（提交前必须通过）
```

- 插件主目录：`plugins/claude/minus-creator/`
- 契约与归属判定：项目根目录上一级的 `CLAUDE.md`（Plugin ↔ Platform 联调契约）
